// src/resources/index.js
// Exports all resource modules for MyPOS SDK

module.exports = function(mypos) {
  // Optionally, initialize or attach resources to the mypos instance here if needed
  // For now, this is a placeholder to satisfy require('./resources')(this) in mypos.js
  // You can expand this as needed for your SDK structure
  return mypos;
}; 